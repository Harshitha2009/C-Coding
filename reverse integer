#include<stdio.h>
int main()
{
    int n,rev=0,r;
    printf("enter n value");
    scanf("%d",&rev);
    while(n!=0)
    {
        r=n%10;
        rev=(rev*10)+r;
        n=(n/10);
    }
    printf("%d",rev);
    return 0;
}
